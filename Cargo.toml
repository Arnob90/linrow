[package]
name = "linrow"
version = "0.2.0"
edition = "2024"
description = "A matrix row reduction library"
license = "MIT"

[dependencies]
derive_more = { version = "2.1.1", features = ["deref", "deref_mut"] }
thiserror = "2.0.18"
