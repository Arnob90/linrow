/// A small constant used for floating-point comparisons.
///
/// This `EPSILON` value helps in comparing `f64` numbers for approximate equality,
/// especially when dealing with results from floating-point arithmetic that
/// might not be exactly equal due to precision issues.
///
/// Current value is `1e-12`.
pub const EPSILON: f64 = 1e-12;
